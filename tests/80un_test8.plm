/*
 * 80UN Test - Absolute minimum
 */

0100H:

MON1: PROCEDURE(FUNC, PARM) EXTERNAL;
    DECLARE FUNC BYTE, PARM ADDRESS;
END MON1;

MAIN: PROCEDURE;
    DECLARE MSG1 DATA('80UN - CP/M Archive Unpacker v1.0', 13, 10, '$');
    CALL MON1(9, .MSG1);
END MAIN;

CALL MAIN;

EOF
